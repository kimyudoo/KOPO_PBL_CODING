#include <stdio.h>

int main()
{
    int linuxScore, javaScore, cScore;
    scanf("%d %d %d", &linuxScore, &javaScore, &cScore);
    printf("Linux : %d\n", linuxScore);
    printf("Java : %d\n", javaScore);
    printf("C : %d\n", cScore);
    return 0;
}

